Run the test suite and summarize the results.

1. Run `python3 test_app.py` from the project root
2. If all tests pass, confirm with a single line: "All tests passed."
3. If any test fails, report:
   - Which test failed
   - The error message
   - A suggested fix
